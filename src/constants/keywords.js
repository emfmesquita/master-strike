const TELEPORT = {
    value: "teleport",
    label: "Teleport"
}
const BRIBE = {
    value: "bribe",
    label: "Bribe"
}

export const keywords = Object.freeze({
    TELEPORT,
    BRIBE
});

export const keywordsArray = Object.freeze([
    TELEPORT,   // 1
    BRIBE       // 2
]);