const ATTACK = "attack";
const RECRUIT = "recruit";
const COST = "cost";
const VP = "vp";

export const icon = Object.freeze({
    ATTACK,
    RECRUIT,
    COST,
    VP
});

export const iconArray = Object.freeze([
    "",
    ATTACK,
    RECRUIT,
    COST,
    VP
]);