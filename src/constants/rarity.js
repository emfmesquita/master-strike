const COMMON = "common";
const UNCOMMON = "uncommon";
const RARE = "rare";

export const rarity = Object.freeze({
    COMMON,
    UNCOMMON,
    RARE
});

export const rarityArray = Object.freeze([
    "",
    COMMON,
    UNCOMMON,
    RARE
]);